message: Just a simple String bean in yaml file!!!!

spring:
  jpa:
    open-in-view: false
    generate-ddl: true
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        default_batch_fetch_size: 20
        jdbc.batch.size: 20
        # id.new_generator_mappings: false  уже не требуется , т.к. для генерации id используется database identity column
    defer-datasource-initialization: true


  h2:
    console:
      enabled: true
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:testdb
    username: sa
    password:

